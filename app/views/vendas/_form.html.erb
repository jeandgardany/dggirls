<%= form_with(model: venda, local: true) do |form| %>
  <% if venda.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(venda.errors.count, "error") %> prohibited this venda from being saved:</h2>

      <ul>
      <% venda.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :data %>
    <%= form.date_select :data %>
  </div>

  <div class="field">
    <%= form.label :valor_venda %>
    <%= form.text_field :valor_venda %>
  </div>

  <div class="field">
    <%= form.label :dinheiro %>
    <%= form.text_field :dinheiro %>
  </div>

  <div class="field">
    <%= form.label :cartao %>
    <%= form.text_field :cartao %>
  </div>

  <div class="field">
    <%= form.label :nota %>
    <%= form.text_field :nota %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
