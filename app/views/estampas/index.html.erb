<p id="notice"><%= notice %></p>

<h1>Estampas</h1>

<table>
  <thead>
    <tr>
      <th>Data</th>
      <th>Data do Corte</th>
      <th>Qtd total</th>
      <th>Valor total</th>
      <th>Estampa tipo</th>
      <th>Finalizado</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @estampas.each do |estampa| %>
      <tr>
        <td><%= estampa.data %></td>
        <td><%= estampa.corte.data %></td>
        <td><%= estampa.qtd_total %></td>
        <td><%= estampa.valor_total %></td>
        <td><%= estampa.estampa_tipo %></td>
        <td><%= formatar_boolean(estampa.finalizado) %></td>
        <td><%= link_to 'Mostrar', estampa %></td>
        <td><%= link_to 'Editar', edit_estampa_path(estampa) %></td>
        <td><%= link_to 'Deletar', estampa, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nova Estampa', new_estampa_path %>
